<%= simple_form_for([@workout, @workout.exercises.build]) do |f| %>
  <%= f.input :name %>
  <%= f.input :sets, placeholder: 'ex: 4' %>
  <%= f.input :reps, placeholder: 'ex: 10' %>
  <br />
  <%= f.button :submit %>
<% end %>